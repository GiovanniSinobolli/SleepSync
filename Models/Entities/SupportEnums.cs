namespace SleepSync.Models.Entities
{
    public class SupportEnums
    {
        public enum Priority { Low = 1, Medium = 2, High = 3 }
        public enum StatusTask { Pending, InProgress, Completed, Cancelled }
        public enum MoodLevel { Terrible = 1, Bad = 2, Neutral = 3, Happy = 4 }
    }
}
